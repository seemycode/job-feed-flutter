name: job_feed_app
description: Get job posts updates
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  equatable: ^2.0.3  
  freezed_annotation:
  dartz:

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: 
  mockito: 
  freezed: 

flutter:
  uses-material-design: true
